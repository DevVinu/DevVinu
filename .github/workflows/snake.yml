name: Generate Snake Animation

on:
  schedule: 
    # Runs at 00:00 UTC every day 
    - cron: "0 0 * * *"
  workflow_dispatch: # Allows you to run it manually

# Explicitly grants permission to push the generated images to your repo
permissions:
  contents: write

jobs:
  build:
    name: Update Contribution Graph
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      # Step 1: Generate the snake animation SVGs
      - name: Generate Snake Animation
        uses: platane/snk@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-contribution-grid-snake.svg?color_snake=#00FFFF&color_dots=#ebedf0,#a3f7b5,#47fc72,#03e33e,#00FFFF
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark&color_snake=#00FFFF&color_dots=#0d1117,#003300,#006600,#009900,#00FFFF
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          
      # Step 2: Push the generated files to the 'output' branch
      - name: Push to Output Branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
